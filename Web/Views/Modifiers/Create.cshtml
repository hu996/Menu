@model RestaurantMenuPlatform.Web.Models.ModifierViewModel
@{ ViewData["Title"] = "Add modifier"; }
<div class="page-header"><span class="eyebrow">Menu configuration</span><h1>Add modifier group</h1><p class="muted">Create a bilingual selection group with database-backed options and price adjustments.</p></div>
<form asp-action="Create" method="post" class="form-stack premium-form">
    <div asp-validation-summary="ModelOnly" class="validation"></div>
    <section class="form-section"><div class="form-section-heading"><span class="eyebrow">Group identity</span><h2>Modifier information</h2><p>Describe the choice group guests will see on the product menu.</p></div><div class="form-grid"><div class="form-field"><label asp-for="Name">English group name</label><input asp-for="Name" /></div><div class="form-field"><label asp-for="NameAr">Arabic group name</label><input asp-for="NameAr" dir="rtl" /></div><div class="form-field form-field-full"><label class="checkbox-label"><input asp-for="IsRequired" /> <span>Selection is required</span></label></div></div></section>
    <section class="form-section"><div class="form-section-heading"><span class="eyebrow">Selection rules</span><h2>Choice limits</h2><p>Set the minimum and maximum selections enforced for this group.</p></div><div class="form-grid"><div class="form-field"><label asp-for="MinSelections"></label><input asp-for="MinSelections" type="number" min="0" inputmode="numeric" /></div><div class="form-field"><label asp-for="MaxSelections"></label><input asp-for="MaxSelections" type="number" min="0" inputmode="numeric" /></div></div></section>
    <fieldset class="form-section"><legend>Options</legend><div class="form-section-heading"><span class="eyebrow">Managed options</span><h2>Guest choices</h2><p>Options are stored separately and can be activated or deactivated.</p></div>@for (var index = 0; index < Model.Options.Count; index++) { <div class="modifier-option-row"><div class="form-grid"><div class="form-field"><label asp-for="Options[index].Name">English option name</label><input asp-for="Options[index].Name" /></div><div class="form-field"><label asp-for="Options[index].NameAr">Arabic option name</label><input asp-for="Options[index].NameAr" dir="rtl" /></div><div class="form-field"><label asp-for="Options[index].PriceAdjustment">Price adjustment</label><input asp-for="Options[index].PriceAdjustment" type="number" step="0.01" inputmode="decimal" /></div><div class="form-field"><label class="checkbox-label"><input asp-for="Options[index].IsActive" /> <span>Active</span></label></div></div></div> }</fieldset>
    @Html.AntiForgeryToken()<div class="form-actions-sticky"><a class="btn btn-secondary" asp-action="Index">Cancel</a><button class="btn btn-primary" type="submit">Create modifier</button></div>
</form>
