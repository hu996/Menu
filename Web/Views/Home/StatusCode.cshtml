@model RestaurantMenuPlatform.Web.Models.ErrorViewModel
@{
    ViewData["Title"] = Model.Title;
}
<section class="error-state">
    <span class="eyebrow">Status @Model.StatusCode</span>
    <h1>@Model.Title</h1>
    <p class="muted">@Model.Message</p>
    @if (Model.StatusCode == 401)
    {
        <a class="btn btn-primary" asp-controller="Account" asp-action="Login">Sign in</a>
    }
    else
    {
        <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Return home</a>
    }
</section>
